import React from 'react';
import { Accordion, Button } from '@/components';

export const UserSpendingRulesAccordion = () => (
  <Accordion title="User Spending Rules">
    <Button onClick={() => console.log('test4234')}>Add</Button>
  </Accordion>
);
